'use strict';

describe('myApp.employee_view module', function() {

  beforeEach(module('myApp.employee_view'));

  describe('employee_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var employeeViewCtrl = $controller('EmployeeViewCtrl');
      expect(employeeViewCtrl).toBeDefined();
    }));

  });
});